node_modules/
backend/data/*.db
backend/data/*.db-*
backend/data/tts-cache/
backend/logs/
*.log
.env
.DS_Store
Thumbs.db

# Runtime data — bot TTS line-pools, dungeon journals, conversation logs.
# These churn constantly during play and were bloating every commit (a one-line
# code change once shipped with 266 KB of mp3 noise). The DB was already
# ignored; now the whole data dir stays out of git.
backend/data/

# crop-save originals + archived token art (backups, not game assets)
public/portraits/*.orig
public/_archive_tokenart/
